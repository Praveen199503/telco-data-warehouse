# ============================================================
# packages.yml
# External DBT packages we depend on
# ============================================================

packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.0.0", "<2.0.0"]